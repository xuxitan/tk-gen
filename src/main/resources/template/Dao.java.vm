package ${daoPackage};

import ${poPackage}.${className};
import java.util.List;
/**
 * ${comments}
 * @date ${datetime}
 */
public interface ${className}Dao{

    /**
     * 根据主键查询单个
     * @param ${classname}
     * @return ${className}
     */
    ${className} query${className}ByPrimaryKey(${className} ${classname});

    /**
     * 根据条件查询单个
     * @param ${classname}
     * @return ${className}
     */
    ${className} query${className}Info(${className} ${classname});

    /**
     * 根据条件查询集合
     * @param ${classname}
     * @return List<${className}>
     */
    List<${className}> query${className}List(${className} ${classname});

    /**
     * 添加单个
     * @param ${classname}
     */
    void add${className}Info(${className} ${classname});

    /**
     * 根据主键修改单个
     * @param ${classname}
     */
    void update${className}ByPrimaryKey(${className} ${classname});

    /**
     * 根据主键删除单个
     * @param ${classname}
     */
    void  delete${className}ByPrimaryKey(${className} ${classname});
}
